import React from "react";
import { View, Text, Button } from "react-native";

const GIBill = ({ navigation }) => {
  return (
    <View>
      <Text>GI Bill</Text>
      {/* <Button title="Go to Home" onPress={() => navigation.navigate("Home")} /> */}
    </View>
  );
};

export default GIBill;
