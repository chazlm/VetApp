import React from "react";
import { View, Text, Button } from "react-native";

const VALoan = ({ navigation }) => {
  return (
    <View>
      <Text>Profile Page</Text>
      {/* <Button title="Go to Home" onPress={() => navigation.navigate("Home")} /> */}
    </View>
  );
};

export default VALoan;
